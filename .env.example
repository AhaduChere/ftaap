# ──────────────────────────────────────────────
#  FTAAP ENVIRONMENT VARIABLES
# ──────────────────────────────────────────────
# Copy this file to `.env` for local development:
#   cp .env.example .env
# Then adjust the values as needed.

# ──────────────
# Database
# ──────────────
# LOCAL DEVELOPMENT (SQLite)
# Prisma will create dev.db automatically in the prisma/ folder.
DATABASE_URL="file:./prisma/dev.db"

# PRODUCTION (PostgreSQL)
# When deploying (Render / Railway / Vercel / etc.), override the above with:
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"
#
# Example:
# DATABASE_URL="postgresql://ftaap_user:strongpassword@db.render.com:5432/ftaap_db?schema=public"

# ──────────────
# Nuxt App Config
# ──────────────
# Used by Nuxt for internal identification
NUXT_ENV_APP_NAME="FTAAP"

# Public API prefix (for frontend API calls)
NUXT_PUBLIC_API_BASE="/api"

# ──────────────
# Optional External Services
# ──────────────
# Add your own API keys or endpoints here if you integrate external tools.
# API_BASE_URL="https://api.example.com"
# API_KEY="your_api_key_here"

# ──────────────
# Deployment Settings (optional)
# ──────────────
# Set NODE_ENV to 'production' in deployed environments
# NODE_ENV="development"
